import { QRGenerator } from './QRGenerator';

export function GenerateRoute() {
  // I'm using the QRGenerator component with default configuration.
  // In the future, I can extend this to accept URL parameters for customization.
  return <QRGenerator />;
}


